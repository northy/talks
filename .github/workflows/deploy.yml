name: Deploy slides on GitHub Pages
on:
  workflow_dispatch:
  push:
    branches: [master]
permissions:
  contents: read
concurrency:
  group: pages
  cancel-in-progress: false
jobs:
  build-knockin-on-headers-door:
    name: Build Knockin' on Header's Door
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./knockin-on-headers-door/presentation
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
      - name: Setup Node JS
        uses: actions/setup-node@v4
        with:
          node-version: 'lts/*'
      - name: Setup @antfu/ni
        run: npm i -g @antfu/ni
      - name: Install dependencies
        run: nci --with-deps
      - name: Build
        run: nr build --base /${{github.event.repository.name}}/knockin-on-headers-door/
      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: knockin-on-headers-door
          path: knockin-on-headers-door/presentation/build
  stage:
    name: Stage all talks for Pages
    runs-on: ubuntu-latest
    needs: [build-knockin-on-headers-door]
    steps:
      - name: Download knockin-on-headers-door
        uses: actions/download-artifact@v4
        with:
          name: knockin-on-headers-door
          path: site/knockin-on-headers-door
      - name: Setup Pages
        uses: actions/configure-pages@v4
      - name: Upload pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site
  deploy:
    name: Deploy on GitHub Pages
    runs-on: ubuntu-latest
    needs: stage
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
